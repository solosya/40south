{% set articleCount = 0 %}
{% set cardIndex = 0 %}


<div class="row" data-section="03">
    <div class="col-12">
        <div class="c-page-head c-page-head-xs">
            <div class="u-heading__large u-heading__large-xs u-pr--6">{{title}}</div>
            {# <a href="{{link}}" class="c-page-head__link">View all</a> #}
        </div>
    </div>
</div>



<div class="row">
    <div class="col-12 u-border-bottom">
        {{this.render('../_single-article.twig', {
            article: articles[articleCount], 
            position: (articleCount+1),
            imageSize: {width: 720, height: 420},
            containerClass: "card-4-mobile card-4-tablet card-4-desktop"
        }) | raw}}
        {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
    </div>

    <div class="col-12 col-md-4">
        <div class="row">
            {% for i in 0..1 if articles[articleCount] %}
                
                <div class="col-12{% if i == 0 %} u-border-bottom{% endif %}">
                    {{this.render('../_single-article.twig', {
                        article: articles[articleCount], 
                        position: (articleCount+1),
                        imageSize: {width: 300, height: 260},
                        containerClass: "card-13-mobile card-13-tablet card-13-desktop"
                    }) | raw}}
                    {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                </div>
            {% endfor %}  
        </div>
        
    </div>


    
</div> <!-- row -->